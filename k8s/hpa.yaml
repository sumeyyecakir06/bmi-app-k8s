apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: bmi-hpa
  namespace: bmi-namespace
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: bmi-deployment
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
